const Validation = (key, value) => {
  switch (key) {
    case 'email':
      return
    case 'password':
      return
    case 'name':
      return
    default:
      return
  }
}

export default Validation
